# Zoe — History

## Project Context

- **Project:** nuget-repo-dashboard — public dashboard tracking NuGet packages and GitHub repos
- **Stack:** C# / .NET testing (xUnit or MSTest), GitHub Actions
- **User:** Bruno Capuano
- **PRD:** `docs/nuget-dashboard-prd-v2.md`
- **Test targets:** C# Collector logic, JSON data contracts, workflow behavior. External APIs should be mocked.

## Learnings

- **Test project location:** `tests/Collector.Tests/Collector.Tests.csproj` referencing `src/Collector/Collector.csproj` (**both net10.0**, retargeted 2026-04-02 from net9.0)
- **Packages used:** xUnit 2.9.3, FluentAssertions 8.3.0, NSubstitute 5.3.0, coverlet 6.0.4
- **HTTP mocking pattern:** Custom `MockHttpMessageHandler` in `Helpers/` — injected into `HttpClient` constructor. URL-keyed responses + custom handler delegate for stateful scenarios (rate-limit retries, call counting).
- **File I/O tests:** Use temp dirs via `Path.GetTempPath()` + unique GUID, cleaned up in `Dispose()`.
- **Framework note:** Environment has .NET 8.0 and 10.0 runtimes only — no 9.0. Both Collector and test project retargeted to `net10.0` for runtime support.
- **ConfigLoader edge cases:** Throws `FileNotFoundException` for missing files, `InvalidOperationException` for empty arrays/null/empty packageIds. Invalid JSON throws `JsonException`.
- **NuGetCollector edge cases:** Per-package error handling — one failure doesn't block others. 404 returns null (skipped). TooManyRequests triggers retry with RetryAfter header. Network errors retry up to 3 times.
- **GitHubCollector edge cases:** Invalid repo format (no `/`) returns null silently. Rate-limit 403 with X-RateLimit-Remaining=0 waits for reset. Handles null optional fields (description, language, license, lastPush).
- **JsonOutputWriter:** Writes to both `data/latest/data.json` and `data/history/YYYY/MM/DD/data.json`. Creates directory trees automatically. Uses `WriteIndented = true` + `camelCase` naming.
- **Test count:** 94 tests across 6 files — ConfigLoader (9), NuGetCollector (10), GitHubCollector (20), JsonOutputWriter (16), Models (20), NuGetProfileDiscoveryService (20 theory expansions).
- **URL parsing patterns:** GitHub repo URL extraction requires handling standard URLs, path suffixes (/tree/main, /blob/master), .git suffixes, and non-GitHub URLs. Key edge cases: malformed URLs (missing owner/repo), null/empty input, non-GitHub domains.
- **Discovery service stub:** Created `NuGetProfileDiscoveryServiceTests.cs` with 20 passing URL parsing tests and commented-out discovery service tests ready for when Kaylee's implementation lands. Uses inline helper method for now that demonstrates the parsing logic.
- **Test file:** `tests/Collector.Tests/NuGetProfileDiscoveryServiceTests.cs` — ready to be updated with actual service tests once `INuGetProfileDiscoveryService`/`NuGetProfileDiscoveryService` are implemented.
- **Kaylee's service (2026-04-02):** Implemented NuGetProfileDiscoveryService with GitHub URL parsing (source-generated regex) and NuGet Search API integration. Zoe's tests now have complete coverage.
- **Kaylee's ignore list (2026-04-02):** Added configurable `ignorePackages` array to dashboard config with case-insensitive filtering in Program.cs pipeline. All 71 tests continue to pass.
- **Split output pipeline (2026-04-02):** Updated tests for Kaylee's split output architecture — two output models (NuGetOutput/RepositoriesOutput) and two-file output (data.nuget.json/data.repositories.json). JsonOutputWriter now has WriteNuGetAsync/WriteRepositoriesAsync replacing WriteAsync. Added 16 tests for split output (8 per file type). All 94 tests pass.
- **Enriched GitHub model (2026-04-02):** GitHubRepoMetrics expanded with 12 new properties: watchersCount (subscribers_count), topics, createdAt, updatedAt, size, defaultBranch, homepage, hasWiki, hasPages, networkCount, visibility, htmlUrl. Updated BuildRepoJson helper with all new fields. Added 9 new GitHubCollector tests to validate parsing of enriched fields, including null-handling tests. Added 3 new Model tests (NuGetOutput, RepositoriesOutput round-trips + enriched fields serialization test). All tests pass — ready for Kaylee's production code.
- **Decision merged (2026-04-02):** Split output decision merged to `decisions.md` as decision #8. Orchestration logs written. Decisions inbox cleaned. Cross-agent coordination complete, all 94 tests passing.
- **Dotted repo name fix (2026-04-02):** Fixed critical regex bug in `NuGetProfileDiscoveryService.ParseGitHubRepo()`. Old regex `[^/?.]+` excluded dots from repo names, breaking ElBruno repos like `ElBruno.ClockTray` → parsed as `ElBruno` only. New regex `[^/?#]+` allows dots but stops at URL delimiters. Added `InternalsVisibleTo` in `Collector.csproj` to test `internal static` method. Added 18 new test cases covering 9 real dotted repos from Bruno's profile + edge cases (dotted repos with paths, .git suffix, query strings, fragments). Test count: 94 → 112. All tests pass.
- **Orchestration coordination (2026-04-02):** Wrote orchestration logs for both Kaylee and Zoe to `.squad/orchestration-log/`. Session log written to `.squad/log/`. Build clean, 112/112 tests passing. End-to-end verified: 16 unique repos discovered (up from 3 due to regex fix), 13 collected (3 rate-limited), 36 packages collected.
- **Configurable NuGet profile tests (2026-07-22):** Created `tests/Collector.Tests/ConfigurationTests.cs` with 28 tests covering: DashboardConfig model (3 tests), JSON deserialization of nugetProfile (6 tests), config resolution precedence via IConfiguration (8 tests), and edge cases (11 tests including Theory expansions). Uses `Microsoft.Extensions.Configuration` in-memory providers to simulate env var > user secrets > config file precedence without touching real environment. Test count: 112 → 140. All tests pass.
- **Config precedence pattern:** The resolution logic `ResolveNuGetProfile(IConfiguration?, DashboardConfig)` checks `IConfiguration["NUGET_PROFILE"]` first (which already layers env var over user secrets), then falls back to `DashboardConfig.NuGetProfile` from JSON. Empty/whitespace env var values are treated as unset and fall through to the config file default.
- **TrendAggregationService tests (2026-07-22):** Created `tests/Collector.Tests/TrendAggregationServiceTests.cs` with 39 tests covering: happy path (3-day aggregation, multiple packages/repos), empty history (no dir, empty dir, nonexistent root), single-day data, missing-day gaps (no interpolation), malformed JSON (corrupt, empty, null arrays — 4 tests), 90-day cap (default window, exact boundary, 120-day bulk, custom window), package appear/disappear mid-history (NuGet + repos), version events (initial, change detection, multi-bump, stable version), DiscoverDateDirectories internal method (sort, cutoff filtering, non-numeric folders, invalid dates), partial-file scenarios (NuGet-only, repos-only), empty/blank identifiers skipped, and TrendData model serialization (property names, round-trip, defaults). Test count: 140 → 179. All tests pass.
- **TrendAggregationService code:** Located at `src/Collector/Services/TrendAggregationService.cs`. Interface `ITrendAggregationService` with `AggregateAsync(repoRoot, windowDays=90)`. Models at `src/Collector/Models/TrendData.cs` (TrendData, PackageTrend, RepositoryTrend, TrendPoint<T>, VersionEvent). Internal method `DiscoverDateDirectories` is testable via InternalsVisibleTo.
- **Trend edge cases discovered:** Service correctly skips empty/whitespace PackageIds and FullNames. Null `packages`/`repositories` arrays in JSON are handled by the `?.` null check after deserialization. FluentAssertions 8.x uses `BeInRange()` instead of `BeLessOrEqualTo`/`BeGreaterOrEqualTo` for numeric assertions.
- **Phase 2 orchestration (2026-04-03):** Completed Phase 2 implementation with Kaylee (TrendAggregationService + dashboard sparklines) and Wash (CI/CD). Commit 77b87ee includes 39 new trend aggregation tests, bringing total to 179 passing. Team orchestration logs written. Decision #16 merged to `decisions.md`.
- **Integration test pattern:** Walk up from `AppContext.BaseDirectory` to find repo root by detecting presence of the `.sln` file (`nuget-repo-dashboard.sln`). Reliable across all build configurations and CI environments.
- **Full pipeline test covers DashboardDataReader → ConfigGenerator → JSON validation:** 6 integration tests in `PipelineIntegrationTests.cs` use real `data/latest/data.repositories.json`. Tests validate: non-empty repo list, per-active-repo file generation, valid JSON structure, deterministic color across two runs, index.json contents, and full output structure (schema URL, version, hex color regex, template contains repo name).
- **RepoIdentity test count:** 19 unit tests + 6 integration tests = 25 tests, all passing on net8.0 and net10.0 (50 total runs).
- **Phase 6 new tests (ConfigGenerator enhanced profile structure):** Added 13 new test cases to `tests/RepoIdentity.Tests/ConfigGeneratorTests.cs` targeting Kaylee's upcoming changes: (1) `GenerateAsync_ProfileHasConsoleTitleTemplate` — checks `console_title_template` key at JSON root containing repo name; (2) `GenerateAsync_SegmentHasSolidBackground` — background must not be `"transparent"` and match `^#[0-9A-Fa-f]{6}$`; (3) `GenerateAsync_SegmentHasContrastForeground` — foreground must be `#FFFFFF` or `#1C1C1C`; (4) `GenerateAsync_IconMatchesPurposeOrLanguage` (9-case Theory) — keyword scan before language fallback, covering mcp/whisper/tts/memory/qrcode/llm/vision/C#/fallback; (5) `GenerateAsync_MultipleReposHaveDistinctColors` — 10 repos produce 10 distinct background hex colors. All 25 existing tests continue to pass; 13 new tests fail expectedly (Kaylee's production implementation pending). Kaylee implemented all features; all 38 tests now pass on both net8.0 and net10.0.
- **Phase 6 completion (2026-07-22):** All 13 pre-written tests now passing after Kaylee's implementation. Profile tests verify: console_title_template key/value, solid background (not transparent), contrast foreground (#FFFFFF/#1C1C1C), purpose icons (keyword-first), and distinct colors across multi-repo set. Total: 25 existing + 13 new = 38 passing tests on both TFMs.
- **Enhanced dashboard metrics tests (2026-07-22):** Created `tests/Collector.Tests/EnhancedTrendAggregationTests.cs` with 19 comprehensive tests for 3 new dashboard features being implemented by Kaylee in parallel: (1) **Download Velocity + Staleness** — 6 tests covering velocity calculation (linear growth, decreasing downloads), stale detection (identical downloads >=3 days), mixed stale/active packages, and edge cases (1-day, 2-day datasets); (2) **Issues Opened/Closed Per Day** — 5 tests covering issue activity aggregation across snapshots, PR filtering, empty repos, GitHubIssue model serialization (State/ClosedAt fields), and ClosedIssuesCount validation; (3) **New Packages/Versions Per Day** — 5 tests covering new package detection on first appearance, version change tracking, no-duplicate-new-packages rule, multiple versions same day, and stable versions (no activity); (4) **Additional edge cases** — 3 tests covering negative velocity, same-issue-multiple-days deduplication, multiple new packages same day. All 19 tests PASS immediately (Kaylee completed implementation before test writing). Test count: 179 → 198.
- **Enhanced trend test patterns:** Used existing TrendAggregationServiceTests.cs patterns — temp directory creation with GUID, CreateHistoryDay helper for date-based directory structure, WriteNuGetSnapshot/WriteReposSnapshot helpers, BuildPackage/BuildRepo/BuildIssue factory methods. FluentAssertions 8.x: use `BeInRange(min, max)` for numeric ranges instead of separate `BeGreaterThan`/`BeLessThan`. New models all present in `TrendData.cs`: PackageVelocity (packageId, avgDailyDownloads, staleDays, isStale), IssueActivityPoint (date, opened, closed), VersionActivityPoint (date, newVersions, packages list), NewPackageEvent (date, packageId). GitHubIssue extended with State (string?) and ClosedAt (DateTimeOffset?). GitHubRepoMetrics extended with RecentClosedIssues (List<GitHubIssue>) and ClosedIssuesCount (int).
- **Test file location:** `tests/Collector.Tests/EnhancedTrendAggregationTests.cs` — 19 tests, all passing. Validates Kaylee's TrendAggregationService enhancements for velocity, staleness, issue activity, version activity, and new package tracking.
- **Dashboard improvements test completion (2026-04-13):** Verified all 19 tests in EnhancedTrendAggregationTests.cs passing after Kaylee's implementation. Tests cover: (1) velocity calculation + staleness detection (6 tests); (2) issue activity aggregation (5 tests); (3) version/package publishing (5 tests); (4) edge cases (3 tests). Kaylee delivered 0 warnings, 0 errors build. All 198 tests pass (179 existing + 19 new).
- **Multi-shard NuGet download count tests (2026-04-13):** Created `tests/Collector.Tests/NuGetCollectorMultiShardTests.cs` with 11 comprehensive tests for Kaylee's dual-shard query implementation. Tests cover: (1) Both shards return data → max aggregation; (2) Shard-specific higher values (USNC > USSC, USSC > USNC); (3) Single shard failures (USNC fails, USSC fails) → fallback to healthy shard; (4) Both shards fail → returns 0; (5) Same value on both shards → returns that value; (6) One shard returns 0, other positive → returns positive; (7) Empty data arrays treated as 0 (USNC empty, USSC empty). Shard URLs: `https://api-v2v3search-0.nuget.org` (USNC) and `https://api-v2v3search-1.nuget.org` (USSC). Tests use same pattern as existing NuGetCollectorTests: MockHttpMessageHandler for HTTP mocking, FluentAssertions for assertions, xUnit for framework. Kaylee's implementation delivered 0 warnings, 0 errors. All 209 tests passing (198 existing + 11 new). ✅
- **Pull Request data collection tests (2026-04-13):** Created `tests/Collector.Tests/GitHubPullRequestTests.cs` with 19 comprehensive tests for Kaylee's full PR data collection feature. Tests organized in 5 categories: (A) Model tests — GitHubPullRequest defaults, serialization round-trip, camelCase JSON property names (3 tests); (B) GitHubRepoMetrics PR fields — new properties exist/default, OpenPullRequests matches list count (2 tests); (C) GitHubCollector PR collection — open PR parsing with full detail, merged PR 30-day filtering, old PR exclusion, empty list handling, null optional fields, draft flag, labels, count matching (9 tests); (D) TrendData PR activity — PullRequestActivityPoint defaults/round-trip, TrendData.PullRequestActivity defaults empty (3 tests); (E) Edge cases — null review decision, branch info parsing (2 tests). Also fixed 14 existing tests in `GitHubCollectorTests.cs` that used old PR endpoint URLs (`/pulls?state=open&per_page=1|100`) — updated to new endpoints (`/pulls?state=open&sort=created&direction=desc&per_page=40` and `/pulls?state=closed&sort=updated&direction=desc&per_page=40`). Kaylee's implementation already landed: GitHubPullRequest model, refactored GitHubCollector with GetRecentPullRequestsAsync/GetRecentMergedPullRequestsAsync, PullRequestActivityPoint in TrendData.cs. Note: additions/deletions/changedFiles are hardcoded to 0 in collector since list endpoint doesn't return them. All 227 tests passing (208 existing + 19 new). ✅
- **PR test collection (2026-04-13):** Updated test suite for Kaylee's PR feature: Created 19 new tests in GitHubPullRequestTests.cs (model deserialization, metrics fields, collector parsing, trend tracking, edge cases). Fixed 14 existing tests in GitHubCollectorTests.cs — migrated mocks from old PR endpoints (/pulls?state=open&per_page=1|100) to new endpoints (/pulls?state=open&sort=created&direction=desc&per_page=40, /pulls?state=closed&sort=updated&direction=desc&per_page=40). Verified line count defaults (0) and BuildPullsJson helper compatibility. All 227 tests passing (208 existing + 19 new). Decision #18 (zoe-pr-tests) merged to decisions.md.
- **MetricsGuardService tests (2026-07-23):** Created 30 tests across two new files for Kaylee's defensive guard layers in `MetricsGuardService`. (1) **MonotonicityGuardTests.cs** — 13 tests: fresh higher (use new), fresh lower (guard activates, keep previous), null previous (first run), empty previous list, new package not in previous, package removed from API (drops off), mixed packages (per-package correct behavior), equal values (no change), zero downloads stays zero, case-insensitive matching, in-place mutation verification, empty fresh list, blank previous PackageId skipped. (2) **StalenessAlertTests.cs** — 17 tests: CountTrailingZeroGrowth helper (4 tests — all increasing=0, all flat=n-1, growth-then-flat=trailing-only, single point=0), CheckStaleness integration (package with growth, 5+ day flatline >100 downloads, below 100 threshold, exactly at 100 boundary, 3-day flat below threshold, new package no history, few data points, exactly 5-day meets threshold, multiple mixed packages, package in trend but not in metrics, decreasing-then-flat, constants validation). Key findings: guard uses `<=` for threshold so exactly 100 is excluded; `CountTrailingZeroGrowth` is `internal static` testable via InternalsVisibleTo; guard mutates fresh list in-place. Test count: 227 → 257. All tests pass.
