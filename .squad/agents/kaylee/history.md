# Kaylee — History

## Project Context

- **Project:** nuget-repo-dashboard — public dashboard tracking NuGet packages and GitHub repos
- **Stack:** C# / .NET, NuGet API, GitHub API, JSON
- **User:** Bruno Capuano
- **PRD:** `docs/nuget-dashboard-prd-v2.md`
- **Collector** lives in `src/Collector/`. Fetches NuGet + GitHub data, outputs to `data/latest/*.json` and `data/history/YYYY/MM/DD/*.json`.

## Learnings

- **Project structure**: `src/Collector/` is a .NET console app (`Collector.csproj`, **retargeted to net10.0**). Models in `Models/`, services in `Services/`.
- **Framework note (2026-04-02):** Environment has .NET 8.0 and 10.0 runtimes only (no 9.0). Collector retargeted from net9.0 to net10.0 for deployment compatibility. No code changes required beyond TFM swap.
- **Serialization**: Using `System.Text.Json` exclusively with `[JsonPropertyName]` attributes on all model properties.
- **Interfaces for testability**: All services expose interfaces (`IConfigLoader`, `INuGetCollector`, `IGitHubCollector`, `IJsonOutputWriter`) for DI and mocking.
- **NuGet API**: Registration endpoint (`registration5-gz-semver2`) for version/metadata + search endpoint for aggregate download counts. Pages may need separate fetching if not inlined.
- **GitHub API**: REST API with optional Bearer token via `GITHUB_TOKEN` env var. Open PR count requires a separate `/pulls?state=open` call.
- **Rate limiting**: Both collectors implement retry logic (3 attempts, exponential backoff). GitHub collector respects `X-RateLimit-Reset` header.
- **Output paths**: `data/latest/data.json` (latest snapshot) and `data/history/{yyyy}/{MM}/{dd}/data.json` (daily history).
- **Repo root resolution**: Program.cs resolves from `AppContext.BaseDirectory` (5 levels up from bin output) or `DASHBOARD_REPO_ROOT` env var.
- **Config**: `config/tracked-packages.json` — validated on load (non-empty packageId required).
- **Key files**: `src/Collector/Program.cs`, `src/Collector/Models/*.cs`, `src/Collector/Services/*.cs`, `config/tracked-packages.json`, `config/dashboard-config.json`, `README.md`.
- **NuGet profile discovery (2026-04-02):** New service `INuGetProfileDiscoveryService` / `NuGetProfileDiscoveryService` in `Services/`. Queries NuGet Search API (`azuresearch-usnc.nuget.org/query?q=owner:{username}`) with pagination to discover all packages for a user profile. Parses GitHub repo URLs from `projectUrl` using source-generated regex.
- **Dashboard config:** `config/dashboard-config.json` contains `nugetProfile` (NuGet username for discovery) and `mergeWithTrackedPackages` (bool to merge static `tracked-packages.json`). Program.cs loads this first; if no profile is set, falls back to tracked-packages.json only.
- **Pipeline flow (6 steps):** 1) Load dashboard config, 2) Discover packages from NuGet profile, 3) Load & merge tracked packages, 4) Collect NuGet metrics, 5) Collect GitHub metrics, 6) Build & write output.
- **GitHub URL parsing:** `NuGetProfileDiscoveryService.ParseGitHubRepo()` handles `github.com/{owner}/{repo}` with optional path/query/`.git` suffix. Uses `[GeneratedRegex]` for perf.
- **Models added:** `DashboardConfig` (config model), `DiscoveredPackage` (intermediate discovery result with `GitHubRepo` field).
- **elbruno profile stats:** 37 packages discovered, 33 with valid GitHub repo URLs, 4 without projectUrl.
- **Zoe testing (2026-04-02):** 20 tests written for GitHub URL parsing (NuGetProfileDiscoveryServiceTests.cs); discovery service tests stubbed pending service completion. 71 total tests passing.
- **Ignore list feature (2026-04-02):** Added `ignorePackages` to `DashboardConfig` and `dashboard-config.json`. Program.cs filters packages after discovery+merge using a case-insensitive `HashSet`. Cleared `tracked-packages.json` of Microsoft packages. Filters `LocalEmbeddings` (bare, non-ElBruno package) from discovery results. Pipeline output: 37 discovered → 1 filtered → 36 collected, all ElBruno-owned.
- **Split output pipeline (2026-04-02):** Refactored output into two separate files: `data.nuget.json` (NuGet package metrics) and `data.repositories.json` (GitHub repository metrics). Created `NuGetOutput` and `RepositoriesOutput` models (kept `DashboardOutput` as legacy for tests). Updated `IJsonOutputWriter` to expose `WriteNuGetAsync()` and `WriteRepositoriesAsync()`. Both files written to `data/latest/` and `data/history/YYYY/MM/DD/`. Program.cs step [6/6] builds both outputs from same `generatedAt` timestamp.
- **Enriched GitHubRepoMetrics (2026-04-02):** Added 12 new fields from GitHub API `/repos/{owner}/{repo}` response: `watchersCount` (actual subscribers), `topics` (array), `createdAt`, `updatedAt`, `size` (KB), `defaultBranch`, `homepage`, `hasWiki`, `hasPages`, `networkCount`, `visibility`, `htmlUrl`. All parsed in `GitHubCollector.CollectRepoAsync()`. Topics require `Accept: application/vnd.github+json` header (already set). Used `subscribers_count` for true watchers (NOT `watchers_count` which aliases stars).
- **Decision merged (2026-04-02):** Split output decision merged to `decisions.md` as decision #8 with full context, rationale, and alternatives. Zoe's test coverage coordinated and passing (94/94 tests). All changes committed to .squad/ orchestration records.
- **Regex bug fix (2026-04-02):** Fixed critical regex bug in `NuGetProfileDiscoveryService.ParseGitHubRepo()`. Previous regex `[^/?.]+` excluded dots from repo names, causing repos like `ElBruno.ClockTray` to parse as `elbruno/ElBruno` instead of `elbruno/ElBruno.ClockTray`. Changed character class to `[^/?#]+` to allow dots while still stopping at path/query/fragment separators. Result: 16 unique repos discovered (up from 3). The `.git` suffix stripping is handled separately in the method.
- **Two-pipeline architecture (2026-04-02):** Restructured `Program.cs` console output to clearly show two pipelines: **Pipeline 1 (Discovery)** with 3 steps [Load config, Discover packages, Build lists] → outputs package/repo counts; **Pipeline 2 (Collection)** with 2 steps [Collect NuGet metrics, Collect GitHub metrics] → outputs to `data.nuget.json` and `data.repositories.json`. This architecture makes the data flow clear: first build deduplicated lists, then collect metrics. Updated README.md to document the two-pipeline model, config system (`dashboard-config.json` with `nugetProfile`/`ignorePackages`), and output files.
- **Zoe's test coordination (2026-04-02):** Zoe added 18 comprehensive tests for dotted repo names and URL edge cases. Tests cover 9 real ElBruno repos (ElBruno.ClockTray, etc.) + edge cases (paths, .git suffix, query/fragment). Used `InternalsVisibleTo` to test `ParseGitHubRepo()` internal method directly. Test count: 94 → 112, all passing. Orchestration logs written.
- **User Secrets support (2026-07-22):** Added .NET User Secrets support for `GITHUB_TOKEN` in `src/Collector/`. Added `Microsoft.Extensions.Configuration`, `Microsoft.Extensions.Configuration.UserSecrets`, and `Microsoft.Extensions.Configuration.EnvironmentVariables` NuGet packages (10.0 preview). Added `UserSecretsId` to csproj. Program.cs now builds an `IConfiguration` from User Secrets + environment variables (env vars override secrets). Used `Assembly.GetExecutingAssembly()` overload because the string-based `AddUserSecrets(string, bool)` didn't resolve correctly in .NET 10 preview. README.md updated with setup instructions. All 112 tests pass. Motivation: Enable ergonomic local development without environment variable setup per session. Secrets stored in OS user profile, outside repository. `optional: true` ensures CI/CD and fresh clones work without secrets file.
- **Configurable NuGet profile (2026-07-22):** Made `nugetProfile` overridable via `NUGET_PROFILE` environment variable or .NET User Secrets. Precedence: env var > user secret > `dashboard-config.json` default. Moved `ConfigurationBuilder` to early in Program.cs (before Pipeline 1) so it's available for both `NUGET_PROFILE` and `GITHUB_TOKEN`. Pipeline 1 step [1/3] now reports both the active profile and its source (`config file`, `environment variable`, or `user secret`). Source detection uses `Environment.GetEnvironmentVariable()` to distinguish env var from user secret (since `IConfiguration` merges both). README.md updated with override instructions, examples, and a single-profile design note. Build passes, all 112 tests pass.
- **HTML Dashboard (2026-04-02):** Created `site/index.html` — single-file HTML dashboard (~400 lines, all CSS/JS inline, no external deps). Fetches `data/data.nuget.json` and `data/data.repositories.json` via relative URLs. Features: NuGet package cards sorted by downloads, GitHub repo cards sorted by stars, dark/light mode via `prefers-color-scheme`, responsive grid layout, CSS variables for theming, loading spinner, error handling, summary cards (package count, repo count, total downloads), tag/topic badges, comma-formatted numbers, NuGet.org and GitHub links, system font stack. Footer links to repo. ~13KB single file. Deployed via Wash's GitHub Pages job in `refresh-metrics.yml`.
- **Dashboard filters & view modes (2026-04-02):** Major update to `site/index.html` (345→808 lines). Added per-section filter toolbars and card/list view toggle. **NuGet filters:** live search by name, sort (downloads/name/newest), min-downloads pill buttons (All/1K+/10K+/100K+). **Repo filters:** live search, sort (stars/forks/issues/PRs/name/updated), language dropdown (auto-populated from data), toggle buttons for "has open issues" and "has open PRs". **View modes:** card (existing grid) and list (compact table) per section, persisted in localStorage. **Architecture:** data stored in JS vars after fetch so filters re-render without re-fetching; `getFilteredPackages()` and `getFilteredRepos()` apply filter+sort then call render functions; `wireFilters()` sets up all event listeners. Shows "Showing X of Y" counts and friendly empty state. Added `--toolbar-bg` CSS variable for dark/light mode filter bars. XSS-safe via `esc()` helper. Responsive: filters wrap on mobile, list view scrolls horizontally. Committed as `74acc94`.

