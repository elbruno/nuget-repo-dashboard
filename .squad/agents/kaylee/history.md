# Kaylee — History

## Project Context

- **Project:** nuget-repo-dashboard — public dashboard tracking NuGet packages and GitHub repos
- **Stack:** C# / .NET, NuGet API, GitHub API, JSON
- **User:** Bruno Capuano
- **PRD:** `docs/nuget-dashboard-prd-v2.md`
- **Collector** lives in `src/Collector/`. Fetches NuGet + GitHub data, outputs to `data/latest/*.json` and `data/history/YYYY/MM/DD/*.json`.

## Learnings

- **Project structure**: `src/Collector/` is a .NET console app (`Collector.csproj`, **retargeted to net10.0**). Models in `Models/`, services in `Services/`.
- **Framework note (2026-04-02):** Environment has .NET 8.0 and 10.0 runtimes only (no 9.0). Collector retargeted from net9.0 to net10.0 for deployment compatibility. No code changes required beyond TFM swap.
- **Serialization**: Using `System.Text.Json` exclusively with `[JsonPropertyName]` attributes on all model properties.
- **Interfaces for testability**: All services expose interfaces (`IConfigLoader`, `INuGetCollector`, `IGitHubCollector`, `IJsonOutputWriter`) for DI and mocking.
- **NuGet API**: Registration endpoint (`registration5-gz-semver2`) for version/metadata + search endpoint for aggregate download counts. Pages may need separate fetching if not inlined.
- **GitHub API**: REST API with optional Bearer token via `GITHUB_TOKEN` env var. Open PR count requires a separate `/pulls?state=open` call.
- **Rate limiting**: Both collectors implement retry logic (3 attempts, exponential backoff). GitHub collector respects `X-RateLimit-Reset` header.
- **Output paths**: `data/latest/data.json` (latest snapshot) and `data/history/{yyyy}/{MM}/{dd}/data.json` (daily history).
- **Repo root resolution**: Program.cs resolves from `AppContext.BaseDirectory` (5 levels up from bin output) or `DASHBOARD_REPO_ROOT` env var.
- **Config**: `config/tracked-packages.json` — validated on load (non-empty packageId required).
- **Key files**: `src/Collector/Program.cs`, `src/Collector/Models/*.cs`, `src/Collector/Services/*.cs`, `config/tracked-packages.json`, `config/dashboard-config.json`, `README.md`.
- **NuGet profile discovery (2026-04-02):** New service `INuGetProfileDiscoveryService` / `NuGetProfileDiscoveryService` in `Services/`. Queries NuGet Search API (`azuresearch-usnc.nuget.org/query?q=owner:{username}`) with pagination to discover all packages for a user profile. Parses GitHub repo URLs from `projectUrl` using source-generated regex.
- **Dashboard config:** `config/dashboard-config.json` contains `nugetProfile` (NuGet username for discovery) and `mergeWithTrackedPackages` (bool to merge static `tracked-packages.json`). Program.cs loads this first; if no profile is set, falls back to tracked-packages.json only.
- **Pipeline flow (6 steps):** 1) Load dashboard config, 2) Discover packages from NuGet profile, 3) Load & merge tracked packages, 4) Collect NuGet metrics, 5) Collect GitHub metrics, 6) Build & write output.
- **GitHub URL parsing:** `NuGetProfileDiscoveryService.ParseGitHubRepo()` handles `github.com/{owner}/{repo}` with optional path/query/`.git` suffix. Uses `[GeneratedRegex]` for perf.
- **Models added:** `DashboardConfig` (config model), `DiscoveredPackage` (intermediate discovery result with `GitHubRepo` field).
- **elbruno profile stats:** 37 packages discovered, 33 with valid GitHub repo URLs, 4 without projectUrl.
- **Zoe testing (2026-04-02):** 20 tests written for GitHub URL parsing (NuGetProfileDiscoveryServiceTests.cs); discovery service tests stubbed pending service completion. 71 total tests passing.
- **Ignore list feature (2026-04-02):** Added `ignorePackages` to `DashboardConfig` and `dashboard-config.json`. Program.cs filters packages after discovery+merge using a case-insensitive `HashSet`. Cleared `tracked-packages.json` of Microsoft packages. Filters `LocalEmbeddings` (bare, non-ElBruno package) from discovery results. Pipeline output: 37 discovered → 1 filtered → 36 collected, all ElBruno-owned.
- **Split output pipeline (2026-04-02):** Refactored output into two separate files: `data.nuget.json` (NuGet package metrics) and `data.repositories.json` (GitHub repository metrics). Created `NuGetOutput` and `RepositoriesOutput` models (kept `DashboardOutput` as legacy for tests). Updated `IJsonOutputWriter` to expose `WriteNuGetAsync()` and `WriteRepositoriesAsync()`. Both files written to `data/latest/` and `data/history/YYYY/MM/DD/`. Program.cs step [6/6] builds both outputs from same `generatedAt` timestamp.
- **Enriched GitHubRepoMetrics (2026-04-02):** Added 12 new fields from GitHub API `/repos/{owner}/{repo}` response: `watchersCount` (actual subscribers), `topics` (array), `createdAt`, `updatedAt`, `size` (KB), `defaultBranch`, `homepage`, `hasWiki`, `hasPages`, `networkCount`, `visibility`, `htmlUrl`. All parsed in `GitHubCollector.CollectRepoAsync()`. Topics require `Accept: application/vnd.github+json` header (already set). Used `subscribers_count` for true watchers (NOT `watchers_count` which aliases stars).
- **Decision merged (2026-04-02):** Split output decision merged to `decisions.md` as decision #8 with full context, rationale, and alternatives. Zoe's test coverage coordinated and passing (94/94 tests). All changes committed to .squad/ orchestration records.
- **Regex bug fix (2026-04-02):** Fixed critical regex bug in `NuGetProfileDiscoveryService.ParseGitHubRepo()`. Previous regex `[^/?.]+` excluded dots from repo names, causing repos like `ElBruno.ClockTray` to parse as `elbruno/ElBruno` instead of `elbruno/ElBruno.ClockTray`. Changed character class to `[^/?#]+` to allow dots while still stopping at path/query/fragment separators. Result: 16 unique repos discovered (up from 3). The `.git` suffix stripping is handled separately in the method.
- **Two-pipeline architecture (2026-04-02):** Restructured `Program.cs` console output to clearly show two pipelines: **Pipeline 1 (Discovery)** with 3 steps [Load config, Discover packages, Build lists] → outputs package/repo counts; **Pipeline 2 (Collection)** with 2 steps [Collect NuGet metrics, Collect GitHub metrics] → outputs to `data.nuget.json` and `data.repositories.json`. This architecture makes the data flow clear: first build deduplicated lists, then collect metrics. Updated README.md to document the two-pipeline model, config system (`dashboard-config.json` with `nugetProfile`/`ignorePackages`), and output files.
- **Zoe's test coordination (2026-04-02):** Zoe added 18 comprehensive tests for dotted repo names and URL edge cases. Tests cover 9 real ElBruno repos (ElBruno.ClockTray, etc.) + edge cases (paths, .git suffix, query/fragment). Used `InternalsVisibleTo` to test `ParseGitHubRepo()` internal method directly. Test count: 94 → 112, all passing. Orchestration logs written.
- **User Secrets support (2026-07-22):** Added .NET User Secrets support for `GITHUB_TOKEN` in `src/Collector/`. Added `Microsoft.Extensions.Configuration`, `Microsoft.Extensions.Configuration.UserSecrets`, and `Microsoft.Extensions.Configuration.EnvironmentVariables` NuGet packages (10.0 preview). Added `UserSecretsId` to csproj. Program.cs now builds an `IConfiguration` from User Secrets + environment variables (env vars override secrets). Used `Assembly.GetExecutingAssembly()` overload because the string-based `AddUserSecrets(string, bool)` didn't resolve correctly in .NET 10 preview. README.md updated with setup instructions. All 112 tests pass. Motivation: Enable ergonomic local development without environment variable setup per session. Secrets stored in OS user profile, outside repository. `optional: true` ensures CI/CD and fresh clones work without secrets file.
- **Configurable NuGet profile (2026-07-22):** Made `nugetProfile` overridable via `NUGET_PROFILE` environment variable or .NET User Secrets. Precedence: env var > user secret > `dashboard-config.json` default. Moved `ConfigurationBuilder` to early in Program.cs (before Pipeline 1) so it's available for both `NUGET_PROFILE` and `GITHUB_TOKEN`. Pipeline 1 step [1/3] now reports both the active profile and its source (`config file`, `environment variable`, or `user secret`). Source detection uses `Environment.GetEnvironmentVariable()` to distinguish env var from user secret (since `IConfiguration` merges both). README.md updated with override instructions, examples, and a single-profile design note. Build passes, all 112 tests pass.
- **HTML Dashboard (2026-04-02):** Created `site/index.html` — single-file HTML dashboard (~400 lines, all CSS/JS inline, no external deps). Fetches `data/data.nuget.json` and `data/data.repositories.json` via relative URLs. Features: NuGet package cards sorted by downloads, GitHub repo cards sorted by stars, dark/light mode via `prefers-color-scheme`, responsive grid layout, CSS variables for theming, loading spinner, error handling, summary cards (package count, repo count, total downloads), tag/topic badges, comma-formatted numbers, NuGet.org and GitHub links, system font stack. Footer links to repo. ~13KB single file. Deployed via Wash's GitHub Pages job in `refresh-metrics.yml`.
- **Dashboard filters & view modes (2026-04-02):** Major update to `site/index.html` (345→808 lines). Added per-section filter toolbars and card/list view toggle. **NuGet filters:** live search by name, sort (downloads/name/newest), min-downloads pill buttons (All/1K+/10K+/100K+). **Repo filters:** live search, sort (stars/forks/issues/PRs/name/updated), language dropdown (auto-populated from data), toggle buttons for "has open issues" and "has open PRs". **View modes:** card (existing grid) and list (compact table) per section, persisted in localStorage. **Architecture:** data stored in JS vars after fetch so filters re-render without re-fetching; `getFilteredPackages()` and `getFilteredRepos()` apply filter+sort then call render functions; `wireFilters()` sets up all event listeners. Shows "Showing X of Y" counts and friendly empty state. Added `--toolbar-bg` CSS variable for dark/light mode filter bars. XSS-safe via `esc()` helper. Responsive: filters wrap on mobile, list view scrolls horizontally. Committed as `74acc94`.
- **Dashboard nav, highlights, collapsible sections (2026-07-22):** Updated `site/index.html` (808→1030 lines, +227 lines). Three features added: **1) Top 3 Highlights section** (`id="top-highlights"`) — shows top 3 NuGet packages by downloads and top 3 GitHub repos by stars in a two-column grid with medal emoji rankings (🥇🥈🥉), compact highlight cards with stat badges. No filters — curated view only. **2) Collapsible sections** — all three sections (Top Highlights, NuGet Packages, GitHub Repos) have clickable headers with chevron indicators (▼/▶). CSS `max-height` transitions for smooth animation. State persisted in `localStorage` keys `collapse-{sectionId}`. Click handler skips `section-meta` child elements (view toggles) so those still work. **3) Sticky nav bar** — `<nav class="top-nav">` with `position: sticky; top: 0` between header and main content. Three pill-style anchor links (🏆 Top 3, 📦 Packages, 🐙 Repositories) with smooth scroll via `html { scroll-behavior: smooth }` and `scroll-margin-top: 60px` on sections. All three features are dark/light mode compatible using existing CSS variables. Responsive: nav shrinks on mobile, highlights stack to single column.
- **CLI mode argument (2026-04-02):** Added `--mode` CLI argument to `Program.cs` supporting two modes: `inventory` and `metrics` (default). **Inventory mode (`--mode inventory`)**: Runs Pipeline 1 (Discovery) only — loads config, discovers packages from NuGet profile, merges tracked packages, filters ignored packages, then writes the deduplicated sorted package list to `config/tracked-packages.json` as properly formatted JSON with `WriteIndented = true`. Skips Pipeline 2 (Collection) entirely. Prints summary showing count and relative path. **Metrics mode (`--mode metrics` or no arg)**: Runs both Pipeline 1 and Pipeline 2 exactly as before — no behavior change. Argument parsing supports both `--mode inventory` and `--mode=inventory` syntax. Invalid mode values return exit code 1 with clear error message. This moves inventory refresh logic from bash scripts (`refresh-inventory.yml`) into the C# Collector where it belongs, eliminating duplication and ensuring consistency with discovery logic. The output format matches existing `PackageConfig[]` structure: sorted by `packageId` (case-insensitive), each entry has `packageId` and `repos` array. Mode is displayed in startup banner. All 112 tests still pass.
- **Historical trend aggregation (2026-07-22):** Phase 2 feature: `TrendAggregationService` scans `data/history/YYYY/MM/DD/` for `data.nuget.json` and `data.repositories.json` snapshots, aggregates into time-series data capped at 90 days. Models in `Models/TrendData.cs`: `TrendData` (root), `PackageTrend` (downloads + version history), `RepositoryTrend` (stars/forks/issues/PRs), `TrendPoint<T>` (date+value), `VersionEvent` (date+version). Service is interface-based (`ITrendAggregationService`) and accepts `repoRoot` for testability. Version tracking detects when a package's latest version changes between snapshots. Output written to `data/latest/data.trends.json` only (not history — trends are derived data). Pipeline 2 now has 3 steps: [1/3] NuGet metrics, [2/3] GitHub metrics, [3/3] Trend aggregation. `IJsonOutputWriter` extended with `WriteTrendsAsync()`. All 140 tests pass.
- **Dashboard sparklines (2026-07-22):** Updated `site/index.html` (1030→1107 lines) with trend visualization. Features: **1) SVG sparklines** on package cards (download trends, 100x30px) and repo cards (star trends); **2) Trend badges** (↑/↓/→ with % change) comparing last 7 days vs prior 7 days; **3) Trends section** at top with total downloads sparkline (200x40px) and top 5 movers by % change; **4) Nav link** for Trends section. Sparklines are pure inline SVG with polyline + area fill, styled with CSS variables for dark/light mode. Trends data fetched optionally — gracefully hidden when `data.trends.json` doesn't exist (first run). No external dependencies. All rendering functions are XSS-safe via `esc()` helper.
- **Phase 2 orchestration (2026-04-03):** Completed Phase 2 implementation with Wash (CI/CD integration) and Zoe (test suite). Commit 6f777a8 includes TrendAggregationService, dashboard sparklines, and full model implementation (502 insertions, 7 files). Decision #16 merged to `decisions.md`. Team orchestration logs written. All 179 tests pass.
- **RepoIdentity Phase 1 scaffold (2026-07-22):** Created new `src/RepoIdentity/` CLI tool and `tests/RepoIdentity.Tests/` project. Paths: `src/RepoIdentity/RepoIdentity.csproj`, `src/RepoIdentity/Program.cs`, `src/RepoIdentity/Commands/GenerateCommand.cs`, `src/RepoIdentity/Commands/PreviewCommand.cs`, `src/RepoIdentity/Commands/ApplyCommand.cs`. CLI uses System.CommandLine beta4 (`2.0.0-beta4.22272.1`) — chosen for .NET 8/10 compat. Multi-targeted `net8.0;net10.0`. AssemblyName `repo-identity` so CLI invoked as `repo-identity`. Three commands: `generate [--source]`, `preview`, `apply [--target]`. All Phase 1 handlers print "not yet implemented". `--target` defaults to `~/.poshthemes`. Tests project references RepoIdentity with `InternalsVisibleTo`. `PlaceholderTests` pass on both TFMs. Both projects added to `nuget-repo-dashboard.sln` under src/tests solution folders. Created `terminal/ohmyposh/.gitkeep` to track the output directory. Decision written to `.squad/decisions/inbox/kaylee-repo-identity-phase1.md`.
- **RepoIdentity Phase 3 (2026-07-22):** Phase 3 models: `RepoIdentityMetadata` (optional `repo.identity.json` structure: Name, Type, AccentColor, Icon) and `EnrichedRepoInfo` (combined base repo data + identity overrides: Owner, Name, FullName, Description, Language, Stars, HtmlUrl, AccentColor, Icon, Type). Both in `src/RepoIdentity/Models/`. `ColorGenerator`: SHA256 hash of seed string → first 3 bytes mapped to 80-210 brightness range → deterministic hex color (e.g. `#A3B4C5`). `MetadataEnricher`: reads `repo.identity.json` from a local repo directory; returns `null` (not throws) when file is missing or malformed JSON. Both services have interfaces (`IColorGenerator`, `IMetadataEnricher`) for DI/testability. 7 new tests in `ColorGeneratorTests.cs` (validity, determinism, distinctness, brightness range) and 3 in `MetadataEnricherTests.cs` (exists/absent/malformed). All 26 RepoIdentity.Tests pass on both net8.0 and net10.0. Decision written to `.squad/decisions/inbox/kaylee-repo-identity-phase3.md`.
- **RepoIdentity Phase 2 — DashboardDataReader (2026-07-22):** Added `src/RepoIdentity/Models/RepositoryInfo.cs` and `src/RepoIdentity/Models/DashboardData.cs` as init-only property records (avoids `[JsonConstructor]` on every record). `RepositoryInfo` maps only the fields needed for Oh My Posh config generation: `Owner`, `Name`, `FullName`, `Description`, `Language`, `Stars`, `LastPush`, `Archived`, `HtmlUrl`. `DashboardData` wraps `GeneratedAt` + `IReadOnlyList<RepositoryInfo>`. Added `src/RepoIdentity/Services/IDashboardDataReader.cs` (interface for testability/DI) and `src/RepoIdentity/Services/DashboardDataReader.cs` (sealed implementation). Deserializes `data/latest/data.repositories.json` using `System.Text.Json` with `PropertyNameCaseInsensitive = true` — no extra NuGet packages needed. Throws `FileNotFoundException` for missing files. Added `tests/RepoIdentity.Tests/DashboardDataReaderTests.cs` with 3 tests: valid parse, file-not-found, nullable fields. All 8 tests pass (4 tests × 2 TFMs). Decision written to `.squad/decisions/inbox/kaylee-repo-identity-phase2.md`.
- **RepoIdentity Phase 4 — ConfigGenerator + generate command (2026-07-22):** Added `GenerationResult` record (`FilesGenerated`, `OutputDirectory`, `GeneratedFiles`). Added `IConfigGenerator` interface and `ConfigGenerator` sealed implementation. Generator iterates non-archived repos, derives deterministic hex color via `IColorGenerator`, picks language emoji icon from a built-in `LanguageIcons` dictionary (fallback `📦`), writes per-repo Oh My Posh JSON config files to `outputDirectory`, and writes `index.json` as a summary manifest. Oh My Posh config uses `$schema` (version 2, single prompt block with text segment). `$schema` key requires a post-serialize string replace because `System.Text.Json` with camelCase policy emits `"schema"` — fixed with `json.Replace("\"schema\":", "\"$schema\":")`. `SanitizeFileName` replaces `/` with `-` (spaces left as-is to match test expectations). `GenerateCommand.cs` fully wired: reads `DashboardDataReader` → `ConfigGenerator`, prints per-file output, defaults `--output` to `terminal/ohmyposh/`. Smoke test on real data: 16 profiles generated. 5 new tests in `ConfigGeneratorTests.cs`; all 19 tests pass on both net8.0 and net10.0. Decision written to `.squad/decisions/inbox/kaylee-repo-identity-phase4.md`.
- **RepoIdentity Phase 6 — Enhanced Profile Generation (2026-07-22):** Phase 6 adds 5 features to `ConfigGenerator.cs`: (1) **`console_title_template`** snake_case key at JSON root; (2) **Solid background** using repo accent color, deterministic contrast foreground (`#FFFFFF` or `#1C1C1C` based on NTSC luminance); (3) **Purpose-based icons** via `PurposeIcons` keyword array + `SelectIcon` internal method scanning repo name before language fallback; (4) **Perceptually-spaced colors** via `EnsureMinDistance` enforcing minimum RGB distance of 60 between repos (max 20 iterations, +30R/+15G shift on collision); (5) **Emoji handling** fixed — `UnescapeSurrogatePairs` regex replaces surrogate pair escapes with literal UTF-8. Updated `docs/repo-identity.md` with full architecture. All public interfaces unchanged; `SelectIcon` is internal for testing via `InternalsVisibleTo`. All 38 tests pass on both net8.0 and net10.0. Zoe's 13 pre-written tests now passing.
- **RepoIdentity Phase 7 — install Command + Install Guide (2026-07-22):** New `InstallCommand.cs` — device bootstrap with 4 steps: (1) Prereq check (`oh-my-posh --version`, 5s timeout, platform-specific install tips on failure); (2) Copy profiles (all `*.json` from `--profiles` dir to `--target`, default `~/.poshthemes`); (3) Copy `Set-RepoTheme.ps1` if present; (4) Idempotent patch of PowerShell `$PROFILE` with 3-line snippet guarded by `# repo-identity:` marker. Platform-aware profile paths: Windows `MyDocuments/PowerShell/Microsoft.PowerShell_profile.ps1`, Unix `~/.config/powershell/Microsoft.PowerShell_profile.ps1`. Options: `--profiles`, `--target`, `--skip-prereqs`, `--dry-run`. Created `docs/repo-identity-install.md` with cross-device install guide covering dependencies, bootstrap steps, machine modifications, idempotency guarantees, dry-run examples, customization, and uninstall. Registered in `Program.cs` via `rootCommand.AddCommand(InstallCommand.Create())`.
- **RepoIdentity Phase 8 — Set-RepoTheme.ps1 Generator (2026-07-22):** Extended `GenerateCommand.cs` to emit PowerShell activation script after JSON generation. Script template stored as C# 11 raw string literal (`"""..."""`) in `GenerateCommand.cs` — keeps version-controlled alongside C# code. Output: `{outputDirectory}/Set-RepoTheme.ps1` with LF-only line endings. Script flow: (1) `git rev-parse --show-toplevel` finds repo root (silent no-op if not in git repo); (2) `git remote get-url origin` extracts remote; (3) Regex match on `github.com[:/](owner/repo)`; (4) Reads `~/.poshthemes/index.json`, looks up matching profile entry; (5) `oh-my-posh init pwsh --config <file> | Invoke-Expression`. `$ErrorActionPreference = 'SilentlyContinue'` makes it a silent no-op in environments missing git/oh-my-posh/index.json. Console output appends `   Set-RepoTheme.ps1` to file listing. Updated `docs/repo-identity.md` with `## Set-RepoTheme.ps1` section. File location: `terminal/ohmyposh/Set-RepoTheme.ps1` (generated, not edited manually).
- **RepoIdentity Phase 5a — preview + apply commands (2026-07-22):** Replaced both stubs with real implementations. `preview` command: reads `data.repositories.json` via `DashboardDataReader`, filters archived repos, prints a formatted table (Repo/Language/Color/Stars columns, 75-char separator) using `ColorGenerator` for deterministic hex colors, reports total count. Uses same `--source` option as `generate` for consistency. `apply` command: `--profiles` defaults to `terminal/ohmyposh/`, `--target` defaults to `~/.poshthemes`, `--repo` for selective single-repo copy (slash→dash sanitization). Copies all non-index `.json` profiles when `--repo` omitted. Guards for missing profiles directory and empty match. Both commands build and smoke test confirmed: 16 profiles listed, build succeeded on net8.0+net10.0. README.md updated with new `## 🎨 repo-identity` section covering all four commands with examples, output description, and optional `repo.identity.json` customization.
- **RepoIdentity Phase 6 — Enhanced Profile Generation (2025-07-22):** Five enhancements to `ConfigGenerator`: (1) `console_title_template` added to root JSON via post-serialize string replace (camelCase→snake_case); (2) solid `Background = color` replaces `"transparent"`, contrasting `Foreground` (`#FFFFFF`/`#1C1C1C`) derived from NTSC luminance; (3) `PurposeIcons` array + `SelectIcon()` method scans repo name for 12 keyword groups before falling back to `LanguageIcons`; (4) `EnsureMinDistance` iterative color-shift algorithm (min Euclidean RGB distance 60, max 20 iterations) pre-generates all colors before writing files; (5) `docs/repo-identity.md` created. Critical lesson: `System.Text.Json` always escapes supplementary Unicode characters (emoji U+10000+) as `\uXXXX\uXXXX` surrogate pairs even with `UnsafeRelaxedJsonEscaping` — added `UnescapeSurrogatePairs()` post-processing with compiled `Regex` to restore literal UTF-8. All 38 tests pass (net8.0 + net10.0). Decision written to `.squad/decisions/inbox/kaylee-phase6-enhancements.md`.
- **RepoIdentity Phase 7 — install command + install guide (2025-07-22):** Created `src/RepoIdentity/Commands/InstallCommand.cs` — the "one-shot device bootstrap" command. Four options: `--profiles` (default: `terminal/ohmyposh`), `--target` (default: `~/.poshthemes`), `--skip-prereqs`, `--dry-run`. Four-step flow: (1) prereq check via `RunProcess("oh-my-posh", "--version")` with platform-specific install hints; (2) copy all `*.json` profile files to target directory; (3) copy `Set-RepoTheme.ps1` if present; (4) idempotent `$PROFILE` patch — appends 3-line snippet if `# repo-identity:` marker not already present, resolves PowerShell profile path per-platform (`MyDocuments/PowerShell/` on Windows, `~/.config/powershell/` on Unix). `Program.cs` updated with `rootCommand.AddCommand(InstallCommand.Create())`. `docs/repo-identity-install.md` created as cross-device install guide (external deps table, bootstrap commands, what changes on machine, idempotency notes, dry-run example, customization, uninstall steps, CLI reference). Build: 0 warnings, 0 errors. `--help` and `--dry-run` both verified working.
- **RepoIdentity Phase 8 — Auto-Detection Script Generation (2025-07-22):** Updated `GenerateCommand.cs` to emit `Set-RepoTheme.ps1` alongside Oh My Posh profile JSON files. Script template stored as a `private const string ScriptTemplate` using a C# 11 raw string literal (`"""..."""`). Written to the output directory with LF-only line endings via `.Replace("\r\n", "\n")`. The script is a cross-platform PowerShell 7+ auto-detection script: uses `git rev-parse --show-toplevel` + `git remote get-url origin` to identify the current repo, looks up `owner/repo` in `~/.poshthemes/index.json`, then calls `oh-my-posh init pwsh --config <file> | Invoke-Expression`. `$ErrorActionPreference = 'SilentlyContinue'` ensures silent no-ops when git/oh-my-posh/index.json are absent. Verified: `generate` command produces `Set-RepoTheme.ps1` with 35 lines and 8 blank lines in `terminal/ohmyposh/`. Updated `docs/repo-identity.md` with a `## Set-RepoTheme.ps1` section covering flow, design decisions, and troubleshooting steps.


